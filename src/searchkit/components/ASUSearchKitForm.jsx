import React from "react";
import PropTypes from 'prop-types';


const ASUSearchKitForm = props => {
    return (
      <div>
        Testing
      </div>
    );
};

ASUSearchKitForm.propTypes = {
    label: PropTypes.node,
    children: PropTypes.node
};

export default ASUSearchKitForm;