import styled from "styled-components";

const LogoWrapper = styled.a`
  display: unset;
  padding: 0;
  margin: 0;
  font-size: unset;
  line-height: unset;
  white-space: unset;
`;

export { LogoWrapper };
