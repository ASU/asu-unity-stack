// @ts-check

/**
 * @typedef {Object} CarouselItem
 * @property {string | number} id
 * @property {JSX.Element} item
 */

/**
 * @typedef {Object} Props
 * @property {number} [perView]
 * @property {string} [width]
 * @property {string} [maxWidth]
 * @property {CarouselItem[]} carouselItems
 * @property {string} [cssClass]
 * @property {JSX.Element} [CustomNavComponent]
 * @property {string} [role]
 * @property {string} [ariaLabelledBy]
 * @property {function(number): void} [onItemClick]
 * @property {boolean} [isFullWidth]
 * @property {boolean} [hasNavButtons]
 * @property {boolean} [hasPositionIndicators]
 * @property {boolean} [removeSideBackground]
 * @property {boolean} [imageAutoSize]
 * @property {boolean} [hasPeek]
 * @property {boolean} [isDraggable]
 * @property {boolean} hasShadow
 */

/**
 * @param {Props} props
 * @returns {JSX.Element}
 */

/**
 * This helps VSCODE and JSOC to recognize the syntax
 * `import(FILE_PATH).EXPORTED_THING`
 *  @ignore
 */
export const JSDOC = "jsdoc";
