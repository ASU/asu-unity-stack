'use strict';

const unityBootstrapTheme = require('..');
const assert = require('assert').strict;

assert.strictEqual(unityBootstrapTheme(), 'Hello from unityBootstrapTheme');
console.info("unityBootstrapTheme tests passed");
