import { document, console } from 'global';
import { storiesOf } from '@storybook/html';
/*
storiesOf('Content/Code', module)
  .addParameters({
    happo: false,
  })

  .add('Inline code', () => `
  For example, <code>&lt;section&gt;</code> should be wrapped as inline.`)

  .add('Code blocks', () => `<pre><code>&lt;p&gt;Sample text here...&lt;/p&gt;
  &lt;p&gt;And another line of sample text here...&lt;/p&gt;</code></pre>`)

  .add('Variables', () => `<var>y</var> = <var>m</var><var>x</var> + <var>b</var>`)

  .add('User input', () => `To switch directories, type <kbd>cd</kbd> followed by the name of the directory.<br>
  To edit settings, press <kbd><kbd>ctrl</kbd> + <kbd>,</kbd></kbd>`)

  .add('Sample output', () => `<samp>This text is meant to be treated as sample output from a computer program.</samp>`);
*/
