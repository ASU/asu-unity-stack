import { Basic } from './global-header.components.js';
import React from 'react';

export default { title: 'Components/Global Header'};


const Template = ({...args}) => (
  <Basic />
);

export const BasicHeader =  Template.bind({});
